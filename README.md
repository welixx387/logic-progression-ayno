# Logic progression ayno

Онлайн-курс логического мышления: **611 задач** в 13 темах на 5 уровнях
сложности — от «Разминки» до «Эксперта». К каждой задаче есть подсказка и
пошаговый разбор.

## Что внутри

- **Главная страница** — описание курса, программа, уровни, вопросы и ответы,
  демо-задача прямо на первом экране.
- **Курс** — 13 тем, в каждой есть короткая теория, разобранный пример и
  задачи пяти уровней. Следующий уровень темы открывается, когда решена
  половина задач предыдущего.
- **Тест уровня** — 15 задач (по 3 на уровень). По результату открываются
  подходящие уровни во всех темах.
- **Тренировка** — случайная подборка по выбранному уровню и темам.
- **Задача дня** — одна и та же для всех в течение дня.
- **Прогресс** — опыт (XP) и ранги, серия дней, карта активности,
  статистика по уровням и темам, достижения, выгрузка и загрузка прогресса,
  светлая и тёмная тема.

Регистрация не нужна: прогресс хранится в `localStorage` браузера.

### Темы

| Тема | Задач |
| --- | --- |
| Числовые ряды | 60 |
| Буквенные ряды | 40 |
| Найди лишнее | 50 |
| Аналогии | 40 |
| Логические выводы (силлогизмы) | 50 |
| Сравнение и порядок | 50 |
| Рыцари и лжецы | 50 |
| Уравнения с символами | 50 |
| Числовые матрицы | 40 |
| Время и календарь | 40 |
| Счёт и комбинаторика | 40 |
| Логические таблицы (задачи Эйнштейна) | 40 |
| Нестандартные задачи | 61 |

## Запуск

```bash
cd logic-progression-ayno
npm install
npm run dev        # http://localhost:5173
npm run build      # сборка в dist/
npm run preview    # просмотр сборки
```

Сайт статический, адреса страниц — через `#` (например, `/#/course`), поэтому
его можно разместить на любом хостинге без настройки сервера.

**Vercel.** Проект лежит в подпапке репозитория, поэтому при создании проекта
в Vercel укажите **Root Directory: `logic-progression-ayno`**. Остальное
Vercel определит сам (Vite, команда `npm run build`, папка `dist`).

## Как устроены задания

Задания не написаны вручную по одному — их создают генераторы в
`scripts/gen/*.ts`, а готовый банк сохраняется в `src/content/tasks.json`.
Генерация детерминированная: при одном и том же коде получаются те же задания
с теми же `id`, поэтому сохранённый прогресс не ломается.

Правильность ответов проверяется при генерации:

- **силлогизмы** — перебором всех диаграмм Эйлера–Венна; объяснения строятся
  как цепочка выводов или как пример-контрпример;
- **рыцари и лжецы**, **порядок**, **логические таблицы** — перебором всех
  вариантов; берутся только задачи с единственным решением;
- **ряды, аналогии, матрицы, «лишнее»** — ответ сверяется с библиотекой
  похожих правил, чтобы у задачи не было второго правдоподобного ответа;
- **нестандартные задачи** (`scripts/gen/classic.ts`) написаны вручную.

После изменения генераторов пересоберите банк:

```bash
npm run tasks
```

Скрипт выводит число заданий по темам и уровням и останавливается с ошибкой,
если найдёт повтор `id`, ответ не из списка вариантов, повтор вариантов и т. п.
Посмотреть задания одной темы в терминале:

```bash
node --experimental-strip-types --no-warnings scripts/show.ts syllogisms 3
```

## Структура

```
scripts/
  generate-tasks.ts   сборка и проверка банка заданий
  gen/                генераторы по темам
  lib/                детерминированный генератор случайных чисел, утилиты
src/
  content/            банк заданий, темы с теорией, уровни и ранги
  pages/              главная, курс, тема, задача, тренировка, тест, прогресс
  components/         карточка задачи, шапка, общие элементы
  store/progress.ts   прогресс, опыт, открытие уровней, достижения
  lib/                роутер, проверка ответов, даты, тема
```
