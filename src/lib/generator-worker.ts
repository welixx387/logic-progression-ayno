/**
 * Фоновый поток с генераторами заданий. На сайте он — отдельный файл и
 * загружается только тогда, когда нужны новые задания. Сборка одностраничной
 * версии подменяет этот модуль вариантом `?worker&inline`.
 */
export { default } from '../generators/worker.ts?worker'
